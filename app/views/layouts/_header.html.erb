    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <a class="navbar-brand" href="#">MyMediumClone</a>
      <ul class="navbar-nav ml-auto">
        <li><%= link_to "Write a story", new_post_path %></li>
        <% if user_signed_in? %>
           <li><%= current_user.email %></li>
        <% end %>
      </ul>
    </nav>
