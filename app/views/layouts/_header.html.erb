    <div class="navbar navbar-expand-lg navbar-light bg-light">
      <a class="navbar-brand" href="#">MyMediumClone</a>
      <ul class="navbar-nav ml-auto">
        <li><%= link_to "Write a story", new_post_path, class: "nav-link" %></li>
        <% if user_signed_in? %>
           <li><%= current_user.email %></li>
        <% else %>
          <li><%= link_to "Sign in / Sign up", new_user_session_path, class: "nav-link" %></li>
        <% end %>
      </ul>
    </div>
